build:
  name: Create a build and run maestro test on it.
  steps:
    - eas/build
    - eas/maestro_test:
        inputs:
          flow_path: |
            maestro/loginScreen_test.yaml
            maestro/signUpScreen_test.yaml
            maestro/dashboardScreen_test.yaml