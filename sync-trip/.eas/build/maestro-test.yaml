build:
  name: Create a build and run maestro test on it.
  steps:
    - eas/build
    - eas/maestro_test:
        inputs:
          flow_path: |
            signUpScreen_test.yaml
            restPwd.yaml
            profileTest.yaml
            tripCreation.yaml
            checklistTest.yaml
            announcementTest.yaml
            billTest.yaml
            routeTest.yaml